# Vercel KV (Redis) Environment Variables
# Required for backend todo list persistence
# Get these values from: Vercel Dashboard → Storage → KV → .env.local tab

# KV REST API URL (required)
KV_REST_API_URL=your_kv_rest_api_url_here

# KV REST API Token (required)
KV_REST_API_TOKEN=your_kv_rest_api_token_here

# KV REST API Read-Only Token (optional, for read-only operations)
# KV_REST_API_READ_ONLY_TOKEN=your_read_only_token_here

# Setup Instructions:
# 1. Create a Vercel KV database in your Vercel project dashboard
# 2. Copy the environment variables from the Vercel dashboard
# 3. Create a `.env.local` file (git-ignored) and paste the values
# 4. For production, add these to Vercel project settings → Environment Variables
#
# See docs/deployment/vercel-kv-setup.md for detailed setup instructions
