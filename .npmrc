# Security defaults for supply chain hardening (ADR-037)
# Blocks lifecycle scripts (preinstall, postinstall) to prevent
# arbitrary code execution from compromised packages
ignore-scripts=true

# Default audit level for local npm audit commands
audit-level=moderate

# Enforce Node.js engine constraints from package.json
engine-strict=true
